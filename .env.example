# ── Base44 (primær AI — udfyld begge for at aktivere) ──────────────────────
BASE44_API_KEY=8ace719fbbf34327bf590e03506f5bfe
BASE44_SUBDOMAIN=raw-vision-lab              # fx: mia  (find det i Base44 Editor → Settings → Deploy)

# ── Groq (fallback — valgfri) ───────────────────────────────────────────────
GROQ_API_KEY=

# ── Server ───────────────────────────────────────────────────────────────────
PORT=3000
