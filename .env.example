# ── Base44 (primær AI — udfyld begge for at aktivere) ──────────────────────
BASE44_API_KEY=din_base44_api_nøgle_her
BASE44_SUBDOMAIN=              # fx: mia  (find det i Base44 Editor → Settings → Deploy)

# ── Groq (fallback — valgfri) ───────────────────────────────────────────────
GROQ_API_KEY=

# ── Server ───────────────────────────────────────────────────────────────────
PORT=3000
